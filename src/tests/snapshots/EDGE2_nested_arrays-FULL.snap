import { z } from "zod"; type User = { name: string; tags: string[][] | null; }; type RootObject = { users: User[]; }; const UserSchema = z.object({ name: z.string(), tags: z.array(z.array(z.string())).nullable() }).strict(); const RootObjectSchema = z.object({ users: z.array(UserSchema) }).strict();